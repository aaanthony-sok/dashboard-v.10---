# dashboard-v.10---

widgets - pract. locat. rev-rank.

## Deployment

This repository is now set up to deploy a static site from `docs/index.html` using GitHub Pages.

### Next steps

1. Replace `docs/index.html` with your dashboard HTML content.
2. Commit and push to `main`.
3. GitHub Actions will automatically deploy the site.

### Notes

- The deployment workflow is at `.github/workflows/pages.yml`.
- Your dashboard should live at `docs/index.html`.
